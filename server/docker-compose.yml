services:
  bamboo-server:
    image: eduard256/bamboo-server:latest
    container_name: bamboo-server
    restart: always
    network_mode: host
    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun:/dev/net/tun
    env_file:
      - .env
    volumes:
      - ./web:/app/web:ro
